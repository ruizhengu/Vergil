FROM ghcr.io/sparfenyuk/mcp-proxy:latest

RUN apk add --no-cache nodejs npm \
    && npm install -g @openzeppelin/contracts-mcp

EXPOSE 8082

ENTRYPOINT ["mcp-proxy", "--host", "0.0.0.0", "--port", "8082", "--", "npx", "@openzeppelin/contracts-mcp"]
